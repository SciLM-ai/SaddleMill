[Main]
executorlib = True
method = SinglePoint
input_format = lmdb
dir_path = ./input_lmdb     # directory of *.aselmdb shards
jobs_per_gpu = 1
Calculator = FAIRChemCalculator

[FAIRChemCalculator]
device = cuda
name_or_path = uma-s-1p2
task_name = omat

[ourSinglePoint]
frames_per_job = 24   # GH200 knee on ~50-atom structures; re-tune if structure size changes substantially